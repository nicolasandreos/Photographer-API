services:
  mysql_database:
    image: mysql:latest
    container_name: photographer_mysql_database

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: photostudio

    ports: 
      - "3310:3306"

    volumes:
      - mysql_volume:/var/lib/mysql

volumes:
  mysql_volume:
    driver: local